import React from 'react';
import {Typography} from "@mui/material/index";

const ProfileContainer = () => {
    return (
        <div>
            <Typography variant={"h2"} >Profile view</Typography>
        </div>
    );
};

export default ProfileContainer;
